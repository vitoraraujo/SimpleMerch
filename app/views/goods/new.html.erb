<h1 align="center">Nova Mercadoria</h1>

<h4 align="center">Dica: Evite cadastrar um produto que você já tenha! Edite o seu estoque! ;) </h4>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <%= form_for @good, html: { autocomplete: "off" } do |f| %>
      <div class="field">
      <%= f.label :description, "Descrição ou Nome:" %>
      <%= f.text_field :description, class: 'form-control' %>
      </div>

      <div class="field">
      <%= f.label :quantity, "Quantidade:" %>
      <%= f.number_field :quantity, class: 'form-control' %>
      </div>

      <div class="field">
      <%= f.label :buy_price, "Preço de compra por unidade:" %>
      <%= f.number_field :buy_price, step: 0.01, class: 'form-control' %>
      </div>

      <div class="field">
      <%= f.label :kind, "Tipo de mercadoria: (Opcional)" %>
      <%= f.text_field :kind, class: 'form-control' %>
      </div>

      <div class="field">
      <%= f.label :buyed_from, "Fornecedor da mercadoria: (Opcional)" %>
      <%= f.text_field :buyed_from, class: 'form-control' %>
      </div>

      <div class="field">
      <%= f.label :buy_day , "Dia da compra:" %>
      <%= f.number_field :buy_day, class: 'form-control' %>
      </div>

      <div class="field">
      <%= f.label :buy_month, "Mês da compra:" %>
      <%= f.number_field :buy_month, class: 'form-control' %>
      </div>

      <div class="field">
      <%= f.label :buy_year, "Ano da compra:" %>
      <%= f.number_field :buy_year, class: 'form-control' %>
      </div>

      <div class="field">
      <%= f.label :note, "Anotações" %>
      <%= f.text_field :note, class: 'form-control' %>
      </div>
      <br>
      <%= f.submit "Cadastrar mercadoria", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>

<h4 style="text-align: center"> Por que não preencher todas as informações da mercadoria? Irá facilitar sua busca posteriormente :D </h4>

<%= link_to "Back", :back %>